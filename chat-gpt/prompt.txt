We show them this. We also offer to write it to a file. We will add that in the future. Now we need to connect to a PDB. So we say enter the PDB service name. We will add .localdomain to the PDB if they don't add that to service name.  So they can add .localdomain or add it. It will get added anyway (or stay the same if they already added it). Then they enter the user. Then we always use password 1234, so we leave out password field. We operate on the assumption password is 1234. We don't need to add password field. So they can connect now. Now that they can connect, 